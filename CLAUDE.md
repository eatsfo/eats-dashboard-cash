# 이 저장소 = 외식사업부 대시보드 "현금" 페이지 (Claude Code 자동 지침)

> 🎨 **화면 스타일은 `DESIGN-SYSTEM.md`를 따른다**(2026-09-03 신설) — 이 페이지는 메인 대시보드에 iframe으로 박혀 보이므로 색·모서리·표 서식이 어긋나면 바로 티가 난다. 고치기 전에 읽을 것.

이 저장소는 `index.html` 하나가 전부다. 이걸 고쳐서 push하면 약 1분 뒤 **https://cash.eats-board.com** 에 자동 반영된다(Cloudflare 자동배포).
담당자는 **코딩 초보**이므로 매 단계 쉽게 설명하고, 담당자가 **"깃허브에 올려줘"**라고 하면 알아서 `git add·commit·push` 한다.

## 🎨 디자인 = Apple 테마 (반드시 이 토큰만 사용)
회사 메인 대시보드와 **똑같은 애플 스타일**로 통일한다. 새로 만들거나 고칠 때 **아래 토큰·규칙을 반드시 따른다.** (index.html의 `:root`에 이미 들어 있음 — 그대로 쓴다.)

**색 토큰**
- 배경 `#edeff3` · 카드 `#ffffff` · 표헤더 `#f5f5f7` · 잉크 `#1d1d1f` · 보조 `#6e6e73` · 옅음 `#86868b` · hairline `#e0e0e0`
- **강조·링크·활성 = 단일 Action Blue `#0066cc`** (연배경 `#e8f1fc`). 🚫 인디고(`#4f46e5`)·보라·슬레이트 액센트 금지.
- 신호등(의미색만): 초록 `#16a34a`/`#15803d`, 빨강 `#dc2626`/`#b91c1c`, 주황 `#d97706`.

**규칙**
- 카드/패널: `#fff` + `1px solid #e0e0e0` + `border-radius:0`(각짐, 2026-07-23 애플 일관성 확정) + **그림자 없음(box-shadow 금지)**. 알약(버튼·칩·태그)만 예외.
- 표 헤더: 배경 `#f5f5f7` + 잉크 글자 + `font-weight:600`.
- 버튼·칩·태그: 알약 `border-radius:9999px`.
- 숫자 셀: `font-variant-numeric:tabular-nums`.
- 폰트: `-apple-system,'SF Pro Text','Inter',system-ui,'Pretendard','Malgun Gothic',sans-serif`.
- 결론/인사이트 박스(`.insight`): 배경 `#f4f8fc` + `1px #e3eaf2` + `radius:0`(패널과 동일). 🚫 좌측 컬러바·비대칭 라운드 금지.
- **옛 색(인디고 `#4f46e5`, 슬레이트 `#0f172a`/`#64748b`/`#f8fafc`/`#eef0fe`/`#e7e9fb`/`#cbd5e1` 등)이 이미 있으면 위 애플 토큰으로 바꾼다.**

## 📊 내용 = 재무팀답게
숫자만 나열하지 말고, 각 표·지표 밑에 **"그래서 무엇을 결정해야 하는지" 결론 한 줄(💡)**을 붙인다. 금액영향(월/연 X억)·시한이 드러나면 더 좋다.

## 담당 페이지 (2026-07-28 갱신 — 4개 페이지, 그중 2개는 메뉴에서 잠시 내림)
파일을 추가·수정하고 push하면 `cash.eats-board.com/<파일명>`으로 자동 배포됩니다. **파일명은 절대 바꾸지 마세요**(메인 대시보드가 이 이름으로 연결).

| 파일 | 메인 대시보드 메뉴 | 상태 |
|---|---|---|
| `index.html` | 현금 씨드머니 (요약 그룹 맨 아래) | 노출 중 |
| `rotc.html` | **사용자본** (재무상태표 분석) | 노출 중 · 2026-07-30 메뉴명 ROTC→사용자본 |
| `invest.html` | 투자심의 (요약) | 노출 중 · ✅ 2026-09-03 딱지 제거 요청(오너 처리 대기) · **2026-09-04 사전 자가점검으로 개편** (아래 참조) |
| `invest-data.json` | — (invest.html이 읽는 데이터) | 화면에 노출되지 않는 데이터 파일. 파일명 고정 |
| `capex.html` | CAPEX (재무상태표 분석) | 노출 중 · ✅ 2026-09-03 딱지 제거 요청(오너 처리 대기) · **2026-09-04 ①~⑤로 재구성** (③ 포트폴리오·⑤ 성적표 카드 유입 · ⑤ 상세표 기본 접힘) |

### ✅ 투자심의·CAPEX "준비중" 딱지 — 제거 요청 (2026-09-03)
페이지 담당자가 **두 페이지가 완성 상태라고 판단**해 딱지 제거를 요청했습니다.
- **딱지는 이 저장소가 아니라 메인 대시보드(별도 저장소)의 메뉴에서 렌더링**됩니다. 이 4개 HTML 파일에는 딱지 마크업이 없으므로, 여기서 지울 수 있는 것이 없습니다.
- 제거는 **메인 대시보드 오너가** 메뉴 설정에서 해야 합니다. 담당자가 오너에게 요청해야 합니다.
- 딱지 기준(①금액영향 ②시한 ③결정 사항) 충족 근거:
  - `capex.html`: ① 한도 249.5억·집행 170.2억·잔여 79.3억 / ③ 26년 포트폴리오 순투입 268.3억 / ④ 미오픈 17건 연환산 +144.5억·26년내 +39.5억 / ⑤ 23~26년 4개 코호트 적중도 카드 + 매장별 상세 · 브랜드 가이드 명시
  - `invest.html`: 브랜드별 자가점검 판정(2607 기준) / 승인 물량 249.5억·순투입 268.3억 / 심의 안건 17건 안건별 투자비·회수목표·오픈예정 / 브랜드별 4년 적중도 + 배분 결론

### 📐 오픈매장피드백 내용의 역할 분담 (2026-09-04)
오픈매장피드백 데이터가 세 군데에 나옵니다. **대상 기간이 달라 숫자가 다르니 합치지 마세요.**

| 화면 | 대상 | 용도 |
|---|---|---|
| `invest.html` Q1 탭 | **최근 12개월** 오픈 35개점 | 자가점검 판정 |
| `capex.html` ⑤ 카드 | 23~26년 4개 코호트 | 브랜드별 추세·배분 가이드(읽는 화면) |
| `capex.html` ⑤ 접힘 영역 | 23~26년 146개점 | 매장 단위 조회(찾아보는 화면) |

`capex.html` ⑤의 상세표는 **`details.fold`로 기본 접힘**입니다. 카드는 항상 보이고 표만 접힙니다. 데이터는 접힌 상태에서도 DOM에 그대로 렌더되므로 필터 버튼은 펼치면 바로 동작합니다. **`open` 속성을 추가해 기본 펼침으로 되돌리지 마세요** — 페이지가 6,000px를 넘어갑니다.

### 🧭 투자심의 페이지 구조 (2026-09-04 개편 — PRD "사내은행 사전 자가점검" 반영)
**로직과 데이터를 분리**했습니다. `invest.html`이 같은 폴더의 `invest-data.json`을 `fetch`로 읽어 화면에서 판정합니다. 월 갱신은 **JSON만 교체**하면 됩니다.

- **탭 6개**: 종합판정 / Q1 투자실행결과 / Q2 질적KPI / Q3 부실매장 / Q5 시드머니 / 심의 안건
- **브랜드 칩 9개**: 전체 · 애슐리 · 리미니 · 피자몰 · 델리 · 로운 · 자연별곡 · 프랑제리 · 기타 (코스트센터 앞 2자리로 귀속)
- **Q4(평당공사비 30/50/80)·Q6(계약상 리스크)은 범위에서 제외**했습니다. 평당 인테리어 비용은 애슐리만 자료가 있어 Q1 주석에 참고로만 남겼습니다. 되살리지 마세요.
- **확정된 기준** — 단위 **억원 유지**(백만원 전환 안 함) · Q1 대상 = **최근 12개월 오픈**(2508~2607) · 영업이익은 **안분후 고정**(안분전 토글 없음) · 시드머니 기본 버전 **지주제출**
- **판정 기준값은 §3.3 확정값**입니다(2026-09-04 확정 — "초안" 문구 제거). `invest-data.json`의 `thresholds`를 고치면 화면 레전드와 모든 배지가 함께 바뀝니다.
- `심의 안건` 탭(미오픈 17건)만 예외적으로 HTML에 숫자가 직접 적혀 있습니다 — 원천이 관리용 이사회승인내역이라 JSON에 넣지 않았습니다.
- **2026-09-04 패널 재배치**: 26년 투자 포트폴리오와 오픈매장 성적표 카드를 `capex.html` ③·⑤로 옮겼습니다. 투자심의에는 **BS분석 요약(종합판정 탭)과 심의 안건 17건**만 남습니다. 역할 분담 = 투자심의는 "올릴 수 있는가·무엇을 올리는가", CAPEX는 "돈이 어떻게 나갔고 결과가 어떤가".

**JSON 재생성 스크립트**: `../build-invest-data.py` (저장소 밖에 둡니다 — 배포 폴더에 넣으면 공개 URL로 노출됩니다). 상위 폴더에서 `python build-invest-data.py` 실행 → `eats-dashboard-cash/invest-data.json` 갱신.

**JSON 재생성 원천 4종** (모두 `../암호화해제/` 아래):
`오픈매장피드백/★★EATS 신규오픈점피드백_2607` [요약]·[CAPEX·오픈매장] → Q1 ·
`매장별 아미스_2607_공유_v2` [26]·[25] → Q2·Q3 ·
`★★외식_현금시드머니_260812_작업용` [7월_260813(지주제출)]·[(브랜드공지)] → Q5 ·
`신규점이사회승인내역관리/★★관리용…_260806` [25년_요약]·[26년_요약] → Q1 이사회 목표(코스트센터 매핑 테이블이 빌드 스크립트에 하드코딩돼 있으니 신규 오픈 시 추가할 것)

**알려진 확정 대기 사항** (화면 주석에도 적혀 있음): 시드머니 버전(현금 페이지는 브랜드공지, 이 페이지는 지주제출이라 숫자가 다름) · 지주제출에 파이낸스 행이 없어 "기타" 판정이 버전에 따라 뒤집힘 · Q1 목표 미설정 3~4개점 · 회전율용 객수 데이터 없음. 판정 경계값(§3.3)은 확정값이므로 대기 목록에서 제외.
- 딱지가 붙었던 이력: 2026-07-28 부착(당시 미완성), 2026-09-03 제거 요청.

## 🤖 데이터 블록 규칙 (2026-07-07 — 본사 대시보드 AI 연동, 자동 준수)
페이지(HTML)를 수정할 때마다, **그 페이지 본문 맨 아래에 아래 형식의 데이터 블록을 실제 값과 동기화**해서 유지하라(없으면 생성). 본사 대시보드 AI가 매주 이 블록을 읽어 경영 인사이트에 반영한다. 화면에는 보이지 않는다.

```html
<script type="application/json" id="fable-data">
{"asof":"YYYY-MM-DD","kpi":{"지표명(단위)":숫자},"risks":["위험 요약 한 줄"],"decisions":["결정 대기 사안 — 시한"]}
</script>
```
- `kpi` = 이 페이지의 핵심 수치 3~6개(페이지 표와 일치) · `risks` = 지금 위험한 것 · `decisions` = 경영진 결정이 필요한 것(시한 포함)
- JSON 유효성 유지(따옴표·쉼표). 페이지가 여러 개면 각 페이지마다 자기 블록.
