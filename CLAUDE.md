# 이 저장소 = 외식사업부 대시보드 "현금" 페이지 (Claude Code 자동 지침)

이 저장소는 `index.html` 하나가 전부다. 이걸 고쳐서 push하면 약 1분 뒤 **https://cash.eats-board.com** 에 자동 반영된다(Cloudflare 자동배포).
담당자는 **코딩 초보**이므로 매 단계 쉽게 설명하고, 담당자가 **"깃허브에 올려줘"**라고 하면 알아서 `git add·commit·push` 한다.

## 🎨 디자인 = Apple 테마 (반드시 이 토큰만 사용)
회사 메인 대시보드와 **똑같은 애플 스타일**로 통일한다. 새로 만들거나 고칠 때 **아래 토큰·규칙을 반드시 따른다.** (index.html의 `:root`에 이미 들어 있음 — 그대로 쓴다.)

**색 토큰**
- 배경 `#edeff3` · 카드 `#ffffff` · 표헤더 `#f5f5f7` · 잉크 `#1d1d1f` · 보조 `#6e6e73` · 옅음 `#86868b` · hairline `#e0e0e0`
- **강조·링크·활성 = 단일 Action Blue `#0066cc`** (연배경 `#e8f1fc`). 🚫 인디고(`#4f46e5`)·보라·슬레이트 액센트 금지.
- 신호등(의미색만): 초록 `#16a34a`/`#15803d`, 빨강 `#dc2626`/`#b91c1c`, 주황 `#d97706`.

**규칙**
- 카드/패널: `#fff` + `1px solid #e0e0e0` + `border-radius:0`(각짐, 2026-07-23 애플 일관성 확정) + **그림자 없음(box-shadow 금지)**. 알약(버튼·칩·태그)만 예외.
- 표 헤더: 배경 `#f5f5f7` + 잉크 글자 + `font-weight:600`.
- 버튼·칩·태그: 알약 `border-radius:9999px`.
- 숫자 셀: `font-variant-numeric:tabular-nums`.
- 폰트: `-apple-system,'SF Pro Text','Inter',system-ui,'Pretendard','Malgun Gothic',sans-serif`.
- 결론/인사이트 박스(`.insight`): 배경 `#f4f8fc` + `1px #e3eaf2` + `radius:0`(패널과 동일). 🚫 좌측 컬러바·비대칭 라운드 금지.
- **옛 색(인디고 `#4f46e5`, 슬레이트 `#0f172a`/`#64748b`/`#f8fafc`/`#eef0fe`/`#e7e9fb`/`#cbd5e1` 등)이 이미 있으면 위 애플 토큰으로 바꾼다.**

## 📊 내용 = 재무팀답게
숫자만 나열하지 말고, 각 표·지표 밑에 **"그래서 무엇을 결정해야 하는지" 결론 한 줄(💡)**을 붙인다. 금액영향(월/연 X억)·시한이 드러나면 더 좋다.

## 담당 페이지 (2026-07-28 갱신 — 4개 페이지, 그중 2개는 메뉴에서 잠시 내림)
파일을 추가·수정하고 push하면 `cash.eats-board.com/<파일명>`으로 자동 배포됩니다. **파일명은 절대 바꾸지 마세요**(메인 대시보드가 이 이름으로 연결).

| 파일 | 메인 대시보드 메뉴 | 상태 |
|---|---|---|
| `index.html` | 현금 씨드머니 (요약 그룹 맨 아래) | 노출 중 |
| `rotc.html` | **사용자본** (재무상태표 분석) | 노출 중 · 2026-07-30 메뉴명 ROTC→사용자본 |
| `invest.html` | 투자심의 (요약) | 노출 중 · ✅ 2026-09-03 딱지 제거 요청(오너 처리 대기) |
| `capex.html` | CAPEX (재무상태표 분석) | 노출 중 · ✅ 2026-09-03 딱지 제거 요청(오너 처리 대기) |

### ✅ 투자심의·CAPEX "준비중" 딱지 — 제거 요청 (2026-09-03)
페이지 담당자가 **두 페이지가 완성 상태라고 판단**해 딱지 제거를 요청했습니다.
- **딱지는 이 저장소가 아니라 메인 대시보드(별도 저장소)의 메뉴에서 렌더링**됩니다. 이 4개 HTML 파일에는 딱지 마크업이 없으므로, 여기서 지울 수 있는 것이 없습니다.
- 제거는 **메인 대시보드 오너가** 메뉴 설정에서 해야 합니다. 담당자가 오너에게 요청해야 합니다.
- 딱지 기준(①금액영향 ②시한 ③결정 사항) 충족 근거:
  - `capex.html`: ① 한도 249.5억·집행 170.2억·잔여 79.3억 / ③ 미오픈 17건 연환산 +144.5억·26년내 +39.5억 / ④ 23~26년 4개 코호트 적중도 + 매장별 상세 · 브랜드 가이드(애슐리 더 투자 / 델리 속도 조절 등) 명시
  - `invest.html`: ① 6대 질문 자가점검(전 항목 2607 기준) / ③ 승인 물량 249.5억·순투입 268.3억 / ④ 심의 안건 17건 안건별 투자비·회수목표·오픈예정 / ⑤ 브랜드별 3년 적중도 + 배분 결론
- 딱지가 붙었던 이력: 2026-07-28 부착(당시 미완성), 2026-09-03 제거 요청.

## 🤖 데이터 블록 규칙 (2026-07-07 — 본사 대시보드 AI 연동, 자동 준수)
페이지(HTML)를 수정할 때마다, **그 페이지 본문 맨 아래에 아래 형식의 데이터 블록을 실제 값과 동기화**해서 유지하라(없으면 생성). 본사 대시보드 AI가 매주 이 블록을 읽어 경영 인사이트에 반영한다. 화면에는 보이지 않는다.

```html
<script type="application/json" id="fable-data">
{"asof":"YYYY-MM-DD","kpi":{"지표명(단위)":숫자},"risks":["위험 요약 한 줄"],"decisions":["결정 대기 사안 — 시한"]}
</script>
```
- `kpi` = 이 페이지의 핵심 수치 3~6개(페이지 표와 일치) · `risks` = 지금 위험한 것 · `decisions` = 경영진 결정이 필요한 것(시한 포함)
- JSON 유효성 유지(따옴표·쉼표). 페이지가 여러 개면 각 페이지마다 자기 블록.
